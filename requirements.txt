# Apate Honeypot - Core Dependencies (Simplified for Quick Setup)

# FastAPI and web server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# HTTP client and utilities
httpx==0.25.2
requests==2.31.0
aiofiles==23.2.1

# Data processing
pydantic==2.5.0
pydantic-settings==2.1.0
python-dateutil==2.8.2

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
ruff==0.1.6

# Basic utilities
python-dotenv==1.0.0

# Optional: Database (install separately if needed)
# sqlalchemy==2.0.23
# psycopg2-binary==2.9.9  # Requires PostgreSQL dev libraries
# redis==5.0.1

# Optional: Security (install separately if needed)  
# passlib[bcrypt]==1.7.4
# python-jose[cryptography]==3.3.0

# Optional: AI dependencies (install separately if needed)
# openai==1.3.5
# anthropic==0.7.7