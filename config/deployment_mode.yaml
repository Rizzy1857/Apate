mode: "data_collection"  # Not "defense"

layer_0:
  enabled: true
  block_traffic: false  # Only tag and route
  
layer_1:
  enabled: true
  influence_responses: false  # Predict only, don't act yet
  
layers_2_4:
  enabled: false  # Wait for clean data
